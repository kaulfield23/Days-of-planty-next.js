import React from 'react';
import { AppBar } from '@mui/material';
import Button from '@mui/material/Button';
import Link from 'next/link';

const Navbar = () => {
  return (
    <AppBar position="sticky" sx={{ backgroundColor: '#99b3a9' }}>
      navbar
    </AppBar>
  );
};

export default Navbar;
