import { AppBar } from '@mui/material';
import Button from '@mui/material/Button';
import Link from 'next/link';
import React from 'react';

const Navbar = () => {
  return (
    <>
      <AppBar position="sticky" sx={{ backgroundColor: '#99b3a9' }}></AppBar>
    </>
  );
};

export default Navbar;
